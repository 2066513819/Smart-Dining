# 前端调用推荐接口说明

推荐规则和权重评分都在**后端（端口 8000）**实现。前端（Vue 或模板页）只需请求后端接口即可拿到「按权重筛选后的推荐结果」。

---

## 一、接口地址

- **基础地址**：`http://localhost:8000`（或你的后端实际地址）
- **生成推荐（供页面「获取推荐」按钮使用）**：`POST /recommendation/generate`

---

## 二、表单页 / 模板页 已对接

当前模板页 `app/templates/recommendation/index.html` 已使用：

- **URL**：`/recommendation/generate`（相对路径，即同一域名下 `http://localhost:8000/recommendation/generate`）
- **方法**：`POST`
- **数据**：表单序列化（`application/x-www-form-urlencoded`）
  - `age_group`：必填，取值 `PRIMARY`（小学）/ `JUNIOR`（初中）/ `SENIOR`（高中）
  - `school`：选填，学校名称
  - `preference`：选填，饮食偏好

后端会按**权重评分规则**（营养 40%、成本 35%、食材种类 25%）生成早/午/晚餐推荐，并返回模板期望的格式。

---

## 三、Vue 中如何调用

在 Vue 里请请求**后端 8000 端口**的同一接口，不要只请求前端自己的 dev 地址。

### 1. 配置后端 baseURL（推荐）

例如在 `vue.config.js` 或环境变量里配置代理，把 `/api` 或 `/recommendation` 指到 8000：

```js
// vue.config.js
module.exports = {
  devServer: {
    proxy: {
      '/recommendation': {
        target: 'http://localhost:8000',
        changeOrigin: true
      },
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  }
}
```

这样在 Vue 里写 `url: '/recommendation/generate'` 时，请求会发到 `http://localhost:8000/recommendation/generate`。

### 2. 直接写完整后端地址

不配置代理时，直接写后端地址：

```js
const response = await fetch('http://localhost:8000/recommendation/generate', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/x-www-form-urlencoded',
    // 若后端支持 JSON，也可用 'Content-Type': 'application/json'
  },
  body: new URLSearchParams({
    age_group: 'JUNIOR',   // PRIMARY | JUNIOR | SENIOR
    school: '某某学校',
    preference: '喜欢清淡'
  }).toString()
});
const data = await response.json();
```

若用 axios：

```js
import axios from 'axios';

const api = axios.create({
  baseURL: 'http://localhost:8000',
  timeout: 15000
});

// 需要登录时，在 headers 里带 token
api.defaults.headers.common['Authorization'] = `Bearer ${token}`;

const { data } = await api.post('/recommendation/generate', 
  new URLSearchParams({
    age_group: 'JUNIOR',
    school: '',
    preference: ''
  }).toString(),
  { headers: { 'Content-Type': 'application/x-www-form-urlencoded' } }
);

// data.recommendation 为 JSON 字符串
const recommendation = JSON.parse(data.recommendation);
// recommendation.breakfast / .lunch / .dinner 为菜品数组
```

### 3. 返回格式（与模板页一致）

```json
{
  "recommendation": "{\"breakfast\":[...],\"lunch\":[...],\"dinner\":[...]}"
}
```

解析 `recommendation` 后：

- `breakfast` / `lunch` / `dinner`：数组，每一项为一道菜
- 每道菜：`{ "name": "菜名", "description": "描述/菜谱摘要", "ingredients": "见菜谱", "nutrition": "热量 xx kcal，蛋白质 xx g，..." }`

---

## 四、其他推荐相关接口（可选）

如需更细控制，可直接调以下接口（均为**后端 8000**）：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v2/recommendation/weighted-scoring` | POST | 单餐权重评分推荐（JSON body） |
| `/api/v2/recommendation/weighted-meal-plan` | POST | 多日权重评分计划（JSON body） |
| `/api/v2/recommendation/weights-info` | GET | 查看当前权重配置（营养 40%、成本 35%、食材 25%） |
| `/api/recommendation/weighted-meal-set-recommendation` | POST | 加权套餐推荐（JSON body） |

权重规则统一在后端 `app/config/recommendation_weights.py` 中配置。

---

## 五、小结

- **推荐规则和权重评分都在后端（8000）**，前端只负责请求和展示。
- 页面上「获取推荐」应请求：**POST http://localhost:8000/recommendation/generate**（或通过代理到该地址）。
- 参数：`age_group`（PRIMARY/JUNIOR/SENIOR）、`school`、`preference`。
- 返回：`{ recommendation: "JSON字符串" }`，解析后得到 `breakfast`、`lunch`、`dinner` 三个数组，用于在 Vue 中渲染早/午/晚餐。
